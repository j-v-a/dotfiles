# modules/homelab/services.nix
# Self-hosted services: Nextcloud, Immich, Home Assistant, Syncthing.
# Populated in Phase 4.
{ ... }:

{
  # Nextcloud — services.nextcloud NixOS module
  # Syncthing — device hub
  # Immich — services.immich (in nixpkgs since 24.05)
  # Home Assistant — services.home-assistant

  # Placeholder — services added during Phase 4
}
